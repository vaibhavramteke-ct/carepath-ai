[pytest]
# CarePath AI test configuration.
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -ra -q
markers =
    unit: fast, isolated unit tests for a single module
    integration: end-to-end tests that exercise the FastAPI app via TestClient
